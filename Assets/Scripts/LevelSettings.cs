using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelSettings : MonoBehaviour
{
    [SerializeField] public int stageOneRequiredCollectibleCounts = 15;
    [SerializeField] public int stageTwoRequiredCollectibleCounts = 15;
    [SerializeField] public int stageThreeRequiredCollectibleCounts = 15;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
